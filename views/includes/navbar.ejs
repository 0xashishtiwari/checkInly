<nav class="navbar navbar-expand-lg ck-navbar sticky-top border-bottom">
  <div class="container-fluid ck-navbar-inner">
    <a class="navbar-brand ck-brand" href="/listings">
      <span class="ck-brand-icon"><i class="fa-regular fa-compass"></i></span>
      <span class="ck-brand-text">CheckInly</span>
    </a>

    <button class="navbar-toggler ck-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ck-nav-links">
        <!-- <a class="nav-link ck-nav-link" href="/listings">Explore</a> -->
        <a class="nav-link ck-nav-link" href="/listings/new">Host your place</a>
      </div>

      <form class="d-flex ck-search-wrap ms-lg-auto" action="/listings" method="GET">
        <input class="form-control ck-search-input" type="search" name="search" placeholder="Search destination"
          aria-label="Search" value="<%= (query && query.search) || '' %>">
        <button class="btn ck-search-btn" type="submit">
          <i class="fa-solid fa-magnifying-glass"></i>
          <span>Search</span>
        </button>
      </form>

      <div class="navbar-nav ck-auth-links ms-lg-3">
        <% if (!currUser) { %>
          <a class="nav-link ck-auth-link" href="/signup">Signup</a>
          <a class="nav-link ck-auth-link ck-auth-login" href="/login">Log In</a>
          <% } else { %>
            <a class="nav-link ck-auth-link" href="/logout">Log out</a>
            <% } %>
      </div>
    </div>
  </div>
</nav>